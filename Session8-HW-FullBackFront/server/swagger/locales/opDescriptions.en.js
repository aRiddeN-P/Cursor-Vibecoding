'use strict';

module.exports = {
  "DELETE /api/admin/expert/recommendations/{id}": "Deletes the recommendation and all related `user_recommendation_status` rows.",
  "DELETE /api/admin/stories/{id}": "Deletes the story record and image file from disk.",
  "DELETE /api/avatar/custom": "Removes the current custom photo from disk and reverts the user avatar to the last selected seed (`avatar_last_seed`).",
  "DELETE /api/messages/{id}": "Only **read** messages can be deleted. Deleting an unread message returns 400.",
  "DELETE /api/profile/devices/{deviceId}": "Removes the device identified by `deviceId` from the connected devices list. Only devices belonging to the current user can be removed.",
  "DELETE /api/push/unsubscribe": "Removes the push subscription from the database for the specified endpoint (current user only). Typically called when the user disables notifications or the browser automatically cancels the subscription.",
  "DELETE /api/transactions/{id}": "The transaction is not physically deleted; only `is_deleted=1` is set and tag `usage_count` values are decremented.",
  "GET /api/admin/admins": "Superadmin only — `password_hash` is never returned.",
  "GET /api/admin/categories/requests": "Returns all category requests with requester user details; pending requests first.",
  "GET /api/admin/expert/recommendations/{id}/stats": "Returns pending/done/dismissed counts and a list of users who marked the recommendation as done.",
  "GET /api/admin/expert/recommendations/subscriber-count": "Used to show notification delivery progress when creating a new recommendation.",
  "GET /api/admin/referrals": "Returns all referral relationships for the admin panel. Names and mobile numbers are masked.",
  "GET /api/admin/referrals/stats": "Global referral statistics for the admin dashboard: total referral relationships, referrals leading to purchases, total discount percentage granted, and the top 10 inviters.",
  "GET /api/admin/stats/overview": "Users, verification, subscriptions, transactions, banners, and goals — from appDb + adminDb.",
  "GET /api/admin/stories": "Returns all stories ordered by `order_index` for the admin panel.",
  "GET /api/admin/subscriptions": "Returns all subscription requests with user details and current subscription status.",
  "GET /api/admin/verifications": "Returns all verification requests with user details, pending first. Requires `req.session.isAdmin === true`.",
  "GET /api/assets": "Calculates the Toman value of each asset from the market cache (Phase 8) or manual price. Creates a daily snapshot if 24 hours have passed.",
  "GET /api/assets/types": "Fixed list of asset types with `market_symbol` and `has_market_price`. Requires login; subscription not required.",
  "GET /api/avatar/list": "Returns all 40 avatars (20 free + 20 premium subscription) with the current user avatar and lock status for each. Before responding, checks subscription expiry so locks are real-time.",
  "GET /api/banners/active": "Banners where `is_active=1` and within the current date range. `impression_count` is incremented non-blocking.",
  "GET /api/categories": "System default categories plus the current user's approved custom categories. Results can be filtered with the `type` parameter.",
  "GET /api/categories/requests": "Returns all category requests submitted by the current user (pending, approved, rejected).",
  "GET /api/expert/recommendations": "Returns active recommendations with per-user status. Sorted: urgent → high → medium → low.",
  "GET /api/messages": "Returns the current user's messages. Before responding, expired messages (`expires_at < now`) are automatically marked as read to enforce the 7-day deletion rule from expiry.\n\nDisplay rules (applied in the same query):\n1) Only this user's messages\n2) Read messages older than 7 days since read are hidden\n3) Read messages older than 2 months are hidden\n4) Expired messages are shown but automatically marked as read.",
  "GET /api/profile": "Returns the current user's full profile with verification level and subscription status. Password is never returned.",
  "GET /api/profile/devices": "Returns devices logged into the account, ordered by most recent activity.",
  "GET /api/profile/invite-code": "Returns the user's unique invite code. Currently formatted as `DKHL-{userId}`.",
  "GET /api/push/vapid-public-key": "VAPID public key used by the frontend to create a browser push subscription. This endpoint is public (no login required). Returns 503 if keys are not configured on the server.",
  "GET /api/referral/discount": "Returns the user's active discount (only `invitee` type on this endpoint — inviter accumulated discount is returned via `GET /api/subscription/status` with key `pending_inviter_discounts`).\n\nInvitee discount is returned only if it has not been used and `expires_at` has not passed.",
  "GET /api/referral/my-invites": "Inviter panel — unique invite code, invite count, earned discounts (up to 5), total accumulated discount for next purchase, and list of invitees with masked names.",
  "GET /api/referral/validate/{code}": "Validates that the invite code matches the `DKHL-{userId}` pattern and the inviter exists in the database. This route is **callable without login** — the signup form calls it on invite code field `blur` to show the inviter's name.\n\nFor privacy, only the inviter's \"first name + last name initial\" is returned (e.g. \"Ali M.\").",
  "GET /api/split/public/{token}": "Shareable link for unregistered members. Returns personal balance when `mobile` query param is provided.",
  "GET /api/stories": "Returns active stories (`is_active = 1`) ordered by `order_index`. Requires user login.",
  "GET /api/stories/status": "Returns the current user's `has_seen_stories` flag so the frontend can decide whether to show stories.",
  "GET /api/subscription/plans": "Returns hardcoded subscription plans from the server. No login required (usable for guest display).",
  "GET /api/subscription/status": "Returns the user's current subscription, remaining days, and pending request (if any).",
  "GET /api/transactions": "Returns the user's transactions with pagination, filters by type/category/tag/date range/month, and text search. Output includes a summary (total income, total expense, balance) for the same filter scope. Default sort: transaction date (desc) then `created_at` (desc).",
  "GET /api/transactions/recurring": "Returns all transactions with `is_recurring=1` (and `is_deleted=0`) along with `next_expected` from the `recurring_alerts` table.",
  "GET /api/transactions/sample-csv": "Returns a CSV file with Persian column headers and sample rows for download. UTF-8 BOM is prepended so Excel opens Persian headers correctly.",
  "GET /api/transactions/summary": "Monthly financial summary: total income, total expense, balance, top 3 expense categories with percentages, and recurring transaction count. Also detects monthly subscriptions as `recurring_subscriptions`.",
  "GET /api/verification/status": "Returns the user's current verification level, pending request (if any), and history for each level.",
  "PATCH /api/admin/categories/defaults/{id}": "Updates name, icon, color, and `is_active` — type cannot be changed.",
  "PATCH /api/admin/categories/requests/{id}": "With `action=approve`, adds a custom category for the user and sends them a message. With `action=reject`, only the status changes to rejected.",
  "PATCH /api/admin/stories/{id}": "Updates `is_active` and/or `order_index`.",
  "PATCH /api/avatar/select": "Activates one avatar from the set of 40. Premium avatars require an active subscription. If a custom photo was set, its file is deleted when an avatar is selected.",
  "PATCH /api/messages/{id}/read": "Marks the specified message as read for the current user. Returns 403 if the message belongs to another user.",
  "PATCH /api/messages/read-all": "Marks all unread messages for the current user as read.",
  "PATCH /api/profile": "Updates user profile fields. Only submitted fields are changed. Some fields are locked based on verification level:\n\n- Email: always read-only\n- Mobile number and national ID: after verification level 1\n- Date of birth: after verification level 2\n- Address and postal code: after verification level 3",
  "PATCH /api/transactions/{id}": "Send only changed fields. Tag `usage_count` is adjusted based on diff. If `is_recurring` changes, the `recurring_alerts` record is synchronized.",
  "POST /api/admin/auth/change-password": "If `must_change_password=1`, `current_password` is not required. New password: minimum 8 characters, one uppercase letter, one digit, one special character.",
  "POST /api/admin/auth/login": "Creates an admin session (`dakhlyar_admin_sid`) with username and password. If `must_change_password=1`, the response includes `must_change: true`.",
  "POST /api/admin/auth/logout": "Destroys the admin session and logs the logout event in `admin_activity_log`.",
  "POST /api/admin/banners": "multipart/form-data — jpg/png/webp, max 3 MB",
  "POST /api/admin/expert/recommendations": "After INSERT, sends push and in-app messages to all users with active subscriptions (fire-and-forget).",
  "POST /api/admin/expert/send": "Resends an active recommendation to all subscribers or one specific user. Only users with `subscription_expires_at > now`.",
  "POST /api/admin/messages/send": "Sends a broadcast message (`target=all`) or direct message (`target=user`). Push notifications are sent non-blocking via `setImmediate`.",
  "POST /api/admin/stories/reset-for-users": "Sets `has_seen_stories` to 0 for all users so stories replay on next login.",
  "POST /api/admin/stories/upload": "Uploads a new story image from admin and saves it to the `stories` table. Allowed formats: jpg, jpeg, png, webp. Max file size: 5 MB. Currently protected by admin placeholder middleware (`req.session.isAdmin === true`) and will connect to admin login in phase 3.",
  "POST /api/admin/subscriptions/{id}/approve": "Approves the request, sets the user's `subscription_plan` and `subscription_expires_at` based on plan duration (expiry = today + `duration_months`), and sends a notification. This operation is atomic.",
  "POST /api/admin/subscriptions/{id}/reject": "Rejects the request; the user's current subscription is unchanged. Sends a notification (with optional reason).",
  "POST /api/admin/verifications/{id}/approve": "Approves the request, increases the user's verification level, and sends a notification. This operation is atomic.",
  "POST /api/admin/verifications/{id}/reject": "Rejects the request; the user's verification level is unchanged. Sends a notification (with reason).",
  "POST /api/auth/check-duplicates": "Checks whether mobile number, email, or national ID are already registered. Called on-blur while filling the signup form.",
  "POST /api/auth/forgot-password": "Receives the user's email, generates a `reset_password` OTP, and sends it by email.",
  "POST /api/auth/login": "Log in with mobile number and password. After 3 failed attempts within 10 minutes, the account is locked for 10 minutes.",
  "POST /api/auth/logout": "Destroys the user session on the server and clears the session cookie. Idempotent — succeeds even if not logged in.",
  "POST /api/auth/register": "Register a new user. Email OTP (type signup) must be verified first; otherwise returns 403.",
  "POST /api/auth/reset-password": "After `reset_password` OTP verification, sets the user's new password.",
  "POST /api/auth/send-otp": "Generates a random 6-digit code and emails it to the user. Code is valid for 180 seconds. Previous unused codes of the same type for this user are automatically invalidated.",
  "POST /api/auth/verify-otp": "Verifies the 6-digit code sent to email. On success, sets session flag `otp_verified_email` so the user can complete signup/password reset.",
  "POST /api/avatar/upload": "Uploads a custom photo (jpg/png/webp, max 3 MB) as the user avatar. Available only for users with active subscriptions. If a previous photo exists, its file is deleted from disk. Stored filename is built from userId and timestamp only (not the uploaded filename) to prevent path traversal.",
  "POST /api/categories/request": "User proposes a custom category; admin approves or rejects it. User cannot have multiple pending requests with the same name.",
  "POST /api/profile/change-password": "Changes the user password. New password must meet complexity rules and cannot match the current password.",
  "POST /api/push/subscribe": "Creates or updates the user's push subscription in the database. If the same endpoint was already registered, it is updated to the new user_id and keys (INSERT OR REPLACE with endpoint as unique key).",
  "POST /api/referral/apply": "After successful `POST /api/auth/register` and receiving `user_id`, the signup form calls this route with the new user ID and valid invite code.\n\n- This route is **callable without session** (login has not occurred yet).\n- Invite code can only be applied at this moment — the user cannot add one later.\n- If the inviter had an active subscription at signup, a `referral_discounts` record (source = `invitee`) valid for 10 days from signup is created for the invitee. This discount is applied to `final_price` when admin approves purchase.\n- Inviter reward is calculated and granted when the invitee's **purchase is approved** (up to 5 successful invites).",
  "POST /api/split/groups/{id}/members": "Group creator only. If mobile exists in users → `is_registered=1`.",
  "POST /api/split/groups/{id}/settle": "With `create_transaction=true`, records an expense transaction for the logged-in user (`from_member`).",
  "POST /api/stories/mark-seen": "After the user views the last story, sets the user's `has_seen_stories` flag to 1. Called without a request body.",
  "POST /api/subscription/request": "Submits a subscription purchase request. Price is always read from server PLANS; client-submitted price is not accepted.",
  "POST /api/transactions": "Records an income or expense transaction. Amount is always stored as a positive integer (Toman). Category must be default or user-owned (approved) and its type (income/expense/both) must match transaction type. Tags sent as array, max 5 tags, 20 characters each. `transaction_date` can be Gregorian or Jalali (YYYY-MM-DD); server auto-detects. Recurring transactions require `recurring_interval`.",
  "POST /api/transactions/bulk-delete": "Accepts a list of IDs and soft-deletes only those belonging to the user. Invalid/other users' IDs are excluded from the final count.",
  "POST /api/transactions/import": "Accepts CSV or Excel (xlsx/xls) with Persian column headers. Max 2 MB. For Excel, the first sheet is processed. Dates written as Jalali YYYY-MM-DD are auto-converted to Gregorian. If category name is not found, default \"Miscellaneous\" is used and returned in warnings. Invalid rows are listed in errors but never cause the entire request to return 500.",
  "POST /api/verification/request": "User can request only the next level (`current_level + 1`). Request is accepted only if required profile fields are complete and no other pending request exists.",
};
